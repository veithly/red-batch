# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# runtime data (owned local store)
/.data/

# env / secrets
.env
.env.*
.dev.vars
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# hackathonhunter internals (not for public repo)
/.hunter/
# internal working area: raw research, drafts, QA artifacts, deck/video, gpt-pro evidence.
# (deck + demo video are linked from README/SUBMISSION instead of committed.)
/pitch/
# stray GPT Pro response dumps that landed at repo root
gpt-pro-response-*.md
# one-off G7 browser-automation helper (Devpost form fill) — internal only
scripts/devpost-fill.mjs
